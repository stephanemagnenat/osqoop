set(Abs_SRCS Abs.cpp)
include_directories (${CMAKE_BINARY_DIR}/processing/Abs)
add_library(Abs MODULE ${Abs_SRCS})
target_link_libraries(Abs Qt5::Core)
install(TARGETS Abs DESTINATION share/osqoop/processing)
